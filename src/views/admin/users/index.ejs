<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>

  <body>
    <%- include('../partials/header', {username: users[0].name}) %>
    <h1>Users List</h1>
    <ul>
      <% for (user of users) { %>
      <li>
        <a href="/admin/users/<%=user.id%>"> <%= user.name %> </a>
      </li>
      <% } %>
    </ul>
    <%-include('../partials/footer') %>
  </body>
</html>
